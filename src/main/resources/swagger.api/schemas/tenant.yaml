Tenant:
  type: object
  properties:
    id:
      type: string
    code:
      type: string
    name:
      type: string
    isCentral:
      type: boolean
  additionalProperties: false
  required:
    - id
    - code
    - name
    - isCentral

TenantCollection:
  type: object
  properties:
    tenants:
      type: array
      description: "Tenants"
      items:
        type: object
        $ref: "tenant.yaml#/Tenant"
    totalRecords:
      type: integer
  additionalProperties: false
  required:
    - tenants
    - totalRecords
